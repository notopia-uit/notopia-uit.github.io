name: CI

on:
  push:
    branches:
      - main
    paths:
      - docs/**
      - index.md
      - package.json
      - pnpm-lock.json
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    services:
      kroki:
        image: yuzutech/kroki
        ports:
          - 8001:8001
    env:
      KROKI_SERVER_URL: http://127.0.0.1:8001
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          cache: pnpm
          node-version-file: package.json
      - name: Restore public cache
        uses: actions/cache/restore@v4
        with:
          path: public
          key: ${{ runner.os }}-public-${{ hashFiles('public/**') }}
          restore-keys: |
            ${{ runner.os }}-public-
      - name: Setup Pages
        uses: actions/configure-pages@v4
      - name: Install dependencies
        run: npm ci
      - name: Build with VitePress
        run: npm run docs:build
      - name: Build again
        run: npm run docs:build
      - name: Save public cache
        uses: actions/cache/save@v4
        with:
          path: public
          key: ${{ runner.os }}-public-${{ hashFiles('public/**') }}
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .vitepress/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    needs: build
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
